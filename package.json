{
  "scripts": {
    "test": "mocha test",
    "test:coverage": "nyc npm test",
    "test:coverage:watch": "nyc npm test -- --watch",
    "success": "cowsay 'jest dobrze'",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "cowsay": "^1.4.0",
    "mocha": "^10.2.0",
    "nyc": "^14.1.1",
    "sinon": "^7.4.1"
  }
}
